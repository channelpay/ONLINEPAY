package patten;

/**
 * 
 * @author 26500
 *  简单的正则表达式记录
 */
public class PattenTest {
    // logger.info("当前登录的管理员手机号是：" + requestMap.get("userphone"));
    // /** 正则验证邮箱 **/
    // String regEx = "[a-zA-Z_]{1,}[0-9]{0,}@(([a-zA-z0-9]-*){1,}\\.){1,3}[a-zA-z\\-]{1,}";
    // /** 正则验证手机 **/
    // String regPx = "^(\\d{3,4}-)?\\d{6,8}$";
    // /** 正则验证url **/
    // String regHx = "http(s)?://([\\w-]+\\.)+[\\w-]+(/[\\w- ./?%&=]*)?";
    //
    // Pattern pattern = Pattern.compile(regPx);
    // Matcher matcher = pattern.matcher(requestMap.get("userphone"));
    // if (!matcher.matches()) {// 正则验证失败
    // logger.info("手机号输入不符合规则：" + requestMap.get("userphone"));
    // }
    // boolean loginBool = bookStoreManagerService.login(managePhone, managePass);
    // if (loginBool) { // 成功登录
    // logger.info(managePhone + "：登录成功：" + LocalDate.now());
    // } else { // 登录失败
    // logger.info(managePhone + "：登录失败：" + LocalDate.now());
    // }
    // ModelAndView moView = new ModelAndView();
    // moView.setViewName("html/manage/ManageMenu");
}
